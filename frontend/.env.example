# ===========================================================================
# Frontend — copiar a `.env.local`. NUNCA versionar el `.env.local`.
#
# Todo lo que lleva el prefijo NEXT_PUBLIC_ se incrusta en el bundle que se
# descarga el navegador: no pongas aquí ningún secreto. Las claves de las APIs
# externas viven solo en el backend.
# ===========================================================================

NEXT_PUBLIC_API_URL=http://localhost:3001/api/v1
